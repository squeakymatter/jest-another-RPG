//This is a very simplified version of the `Potion()` constructor written earlier. Mock doesn't need to incorporate any random logic. It just needs to return valid values that the `Player` object can later use.

module.exports = function () {
  this.name = 'health'
  this.value = 20
}
